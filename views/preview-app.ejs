<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, viewport-fit=cover, user-scalable=no">
    <meta name="theme-color" content="#d7cede">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%
        const ogImageUrl = (typeof ogPreviewImage !== 'undefined' && ogPreviewImage) ? ogPreviewImage : previewImage;
        const safeCanonical = (typeof canonicalLink !== 'undefined' && canonicalLink) ? canonicalLink : '';
        const storeName = (typeof store !== 'undefined' && store && store.name) ? String(store.name).trim() : '';
        const metaDescription = (function() {
            var s = function(v) { return (v || '').split(',')[0].trim(); };
            return [
                item.categoryName,
                item.size && ('Size: ' + s(item.size)),
                item.color && ('Color: ' + s(item.color)),
                item.qty && ('Qty: ' + s(item.qty))
            ].filter(Boolean).join(' | ') || 'View product and chat with vendor';
        })();
    %>
    <title><%= storeName ? (storeName + ' | ') : '' %><%= item.price %> - Product</title>
    <meta name="description" content="<%= metaDescription %>">
    <% if (safeCanonical) { %><link rel="canonical" href="<%= safeCanonical %>"><% } %>
    <meta property="og:title" content="<%= storeName ? (storeName + ' | ') : '' %>Price: <%= item.price %>">
    <meta property="og:description" content="<%= metaDescription %>">
    <meta property="og:image" content="<%= ogImageUrl %>">
    <meta property="og:image:secure_url" content="<%= ogImageUrl %>">
    <meta property="og:image:width" content="1200">
    <meta property="og:image:height" content="1200">
    <meta property="og:type" content="product">
    <% if (safeCanonical) { %><meta property="og:url" content="<%= safeCanonical %>"><% } %>
    <meta property="og:site_name" content="<%= storeName || 'Products' %>">
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="<%= storeName ? (storeName + ' | ') : '' %>Price: <%= item.price %>">
    <meta name="twitter:description" content="<%= metaDescription %>">
    <meta name="twitter:image" content="<%= ogImageUrl %>">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        :root {
            --page-bg: #d7cede;
            --app-bg: #d7cede;
            --surface: #f8f6fb;
            --surface-secondary: #f0ecf7;
            --separator: rgba(53,38,78,0.12);
            --text: #271f35;
            --text-secondary: #5c5369;
            --text-tertiary: #7a6f8a;
            --tint: #6a59eb;
            --green: #34c759;
            --radius: 16px;
            --radius-lg: 22px;
            --tab-height: 56px;
            --header-height: 48px;
            --media-zoom: 1;
        }
        * { margin: 0; padding: 0; box-sizing: border-box; -webkit-tap-highlight-color: transparent; }
        html { height: 100%; }
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Outfit', sans-serif;
            background: var(--page-bg);
            color: var(--text);
            min-height: 100%;
            min-height: 100dvh;
            padding-bottom: calc(var(--tab-height) + env(safe-area-inset-bottom));
            -webkit-font-smoothing: antialiased;
        }

        .app-header {
            position: sticky;
            top: 0;
            z-index: 10;
            height: calc(var(--header-height) + env(safe-area-inset-top));
            padding-top: env(safe-area-inset-top);
            padding-left: 20px;
            padding-right: 20px;
            background: var(--app-bg);
            display: flex;
            align-items: center;
            justify-content: center;
            border-bottom: 1px solid var(--separator);
        }
        .app-title {
            font-size: 17px;
            font-weight: 600;
            letter-spacing: -0.03em;
            color: var(--text);
        }
        .app-content {
            width: 100%;
            padding: 12px 0 118px;
            max-width: 100vw;
            margin: 0 auto;
            display: flex;
        }

        .immersive-stage {
            position: relative;
            width: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            flex: 1;
            min-height: clamp(540px, calc(100dvh - 210px), 760px);
            padding: 24px 0 24px;
        }
        .stage-head {
            width: 90vw;
            max-width: 90vw;
            margin-top: 8px;
            margin-bottom: 12px;
            flex-shrink: 0;
        }
        .result-label {
            font-size: 11px;
            font-weight: 600;
            color: var(--text-tertiary);
            letter-spacing: 0.12em;
            text-transform: uppercase;
            margin-bottom: 4px;
        }
        .result-count {
            font-size: 22px;
            font-weight: 800;
            color: var(--text);
            letter-spacing: -0.04em;
            margin-bottom: 10px;
        }
        .progress-dots {
            display: flex;
            gap: 6px;
            margin-bottom: 0;
        }
        .progress-dots span {
            width: 6px;
            height: 6px;
            border-radius: 50%;
            background: rgba(106,89,235,0.3);
            transition: all 0.25s ease;
        }
        .progress-dots span.active {
            background: var(--tint);
            transform: scale(1.25);
            box-shadow: 0 0 0 2px rgba(106,89,235,0.2);
        }

        .stack-wrapper {
            position: relative;
            width: 90vw;
            max-width: 90vw;
            display: flex;
            justify-content: center;
            flex: 0 0 auto;
            min-height: 0;
            padding-bottom: 166px;
            touch-action: pan-y;
        }
        @media (max-height: 760px) {
            .immersive-stage {
                justify-content: flex-start;
                min-height: 0;
                padding-top: 14px;
            }
        }
        .stack-container {
            position: relative;
            width: 100%;
            max-width: 100%;
            aspect-ratio: 9 / 16;
            transform: translateX(-14px);
            overflow: visible;
            margin-left: 14px;
        }
        .ghost-card {
            position: absolute;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            border-radius: var(--radius-lg);
            background: rgba(248,246,251,0.9);
            border: 1px solid rgba(53,38,78,0.08);
            pointer-events: none;
        }
        .ghost-card.back-1 {
            transform: translate3d(34px, 12px, 0) scale(0.95, 0.915);
            box-shadow: 0 8px 20px rgba(41,27,64,0.12), 0 2px 8px rgba(41,27,64,0.06);
            border-color: rgba(53,38,78,0.1);
            filter: brightness(0.98) saturate(0.98);
        }
        .ghost-card.back-2 {
            transform: translate3d(62px, 24px, 0) scale(0.9, 0.83);
            box-shadow: 0 6px 16px rgba(41,27,64,0.1), 0 2px 6px rgba(41,27,64,0.05);
            border-color: rgba(53,38,78,0.12);
            filter: brightness(0.96) saturate(0.96);
        }

        .product-card {
            position: absolute;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            z-index: 3;
            background: var(--surface);
            border-radius: var(--radius-lg);
            border: 1px solid rgba(53,38,78,0.08);
            box-shadow: 0 12px 32px rgba(41,27,64,0.15), 0 4px 12px rgba(41,27,64,0.08);
            overflow: hidden;
        }
        .card-image-fill {
            position: absolute;
            inset: 0;
            overflow: hidden;
            background: linear-gradient(180deg, #eee8f4 0%, #e5deef 100%);
        }
        .card-image-fill .card-image-bg,
        .card-image-fill .card-image-main,
        .card-image-fill .card-video-main {
            position: absolute;
            inset: 0;
            width: 100%;
            height: 100%;
            display: block;
            border: 0;
        }
        .card-image-fill .card-image-bg {
            object-fit: cover;
            object-position: center;
            filter: blur(20px) saturate(1.06);
            transform: scale(1.08);
            opacity: 0.52;
        }
        .card-image-fill .card-image-main {
            object-fit: contain;
            object-position: center;
            transform: scale(var(--media-zoom));
            transform-origin: center;
            pointer-events: none;
        }
        .card-image-fill .card-image-bg {
            pointer-events: none;
        }
        .card-image-fill .card-video-main {
            object-fit: contain;
            object-position: center;
            background: linear-gradient(180deg, #ede8f5 0%, #e5dff0 100%);
            pointer-events: auto;
        }
        .card-image-fill::after {
            content: '';
            position: absolute;
            inset: 0;
            pointer-events: none;
            background: linear-gradient(
                180deg,
                rgba(24, 17, 38, 0.08) 0%,
                rgba(24, 17, 38, 0.02) 34%,
                rgba(24, 17, 38, 0.24) 100%
            );
        }

        .card-top-row {
            position: absolute;
            top: 18px;
            left: 18px;
            right: 18px;
            z-index: 4;
            display: flex;
            align-items: center;
            justify-content: space-between;
            pointer-events: none;
        }
        .card-label {
            display: inline-flex;
            align-items: center;
            padding: 7px 10px;
            min-height: 30px;
            border: 1px solid rgba(110, 131, 160, 0.7);
            background: rgba(244, 246, 249, 0.75);
            color: #1b2434;
            font-size: 12px;
            font-weight: 600;
            letter-spacing: 0.01em;
            border-radius: 0;
        }
        .card-label.is-empty {
            visibility: hidden;
        }
        .card-heart {
            width: 24px;
            height: 24px;
            color: #1b2434;
            opacity: 0.94;
        }
        .card-bottom-meta {
            position: absolute;
            left: 18px;
            right: 18px;
            bottom: 18px;
            z-index: 4;
            display: flex;
            flex-direction: column;
            gap: 2px;
            pointer-events: none;
        }
        .card-price-inline {
            font-size: 42px;
            line-height: 1;
            font-weight: 800;
            color: #f4f7fb;
            text-shadow: 0 2px 14px rgba(16, 20, 31, 0.45);
            letter-spacing: -0.04em;
        }
        .card-name-inline {
            font-size: 15px;
            font-weight: 500;
            color: rgba(246, 248, 252, 0.94);
            text-shadow: 0 1px 10px rgba(16, 20, 31, 0.45);
            letter-spacing: -0.01em;
        }

        .stack-overlay {
            position: absolute;
            inset: 0;
            z-index: 12;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            padding: 0;
            pointer-events: none;
            border-radius: var(--radius-lg);
            background: linear-gradient(180deg, transparent 52%, rgba(28,20,44,0.06) 100%);
        }
        .stack-spacer {
            flex: 1;
            min-height: 0;
            pointer-events: none;
        }
        .stack-footer {
            pointer-events: auto;
            transform: translateY(calc(100% + 14px));
            margin-bottom: 8px;
        }
        .card-details {
            margin-top: 0;
            padding: 0 2px;
            pointer-events: none;
        }
        .card-title {
            font-size: 34px;
            line-height: 1.1;
            font-weight: 800;
            color: var(--text);
            letter-spacing: -0.04em;
        }
        .card-subtitle {
            font-size: 12px;
            font-weight: 500;
            color: var(--text-tertiary);
            margin-top: 8px;
            line-height: 1.4;
            letter-spacing: 0.01em;
        }

        .action-row {
            display: grid;
            grid-template-columns: 0.9fr 1.03fr 1.1fr;
            gap: 12px;
            margin-top: 18px;
        }
        .action-row button {
            min-width: 0;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 6px;
            padding: 12px 10px;
            min-height: 52px;
            border-radius: 18px;
            border: 1px solid rgba(255,255,255,0.54);
            background: linear-gradient(140deg, rgba(255,255,255,0.4), rgba(255,255,255,0.2));
            backdrop-filter: blur(18px) saturate(165%);
            -webkit-backdrop-filter: blur(18px) saturate(165%);
            box-shadow: inset 0 1px 0 rgba(255,255,255,0.58), 0 10px 22px rgba(41,27,64,0.12);
            font-size: 12.2px;
            font-weight: 700;
            line-height: 1;
            white-space: nowrap;
            cursor: pointer;
            font-family: inherit;
            transition: transform 0.16s ease, background-color 0.22s ease, border-color 0.22s ease, color 0.22s ease;
            pointer-events: auto;
        }
        .action-row button .icon { width: 15px; height: 15px; flex-shrink: 0; }
        .action-row button:active { opacity: 0.93; transform: scale(0.985); }
        .btn-copy {
            background: linear-gradient(145deg, rgba(255,255,255,0.48), rgba(255,255,255,0.28));
            color: #594f6a;
            border-color: rgba(255,255,255,0.62);
        }
        .wa-btn {
            background: linear-gradient(145deg, rgba(56,203,102,0.34), rgba(255,255,255,0.22));
            color: #1a7641;
            border-color: rgba(56,203,102,0.4);
        }
        .btn-cart {
            background: linear-gradient(145deg, rgba(106,89,235,0.34), rgba(255,255,255,0.2));
            color: #4636bb;
            border-color: rgba(106,89,235,0.42);
        }
        @media (max-width: 360px) {
            .action-row {
                grid-template-columns: repeat(3, minmax(0, 1fr));
                gap: 8px;
            }
            .action-row button {
                font-size: 11px;
                padding: 11px 8px;
                min-height: 48px;
            }
            .action-row button .icon {
                display: none;
            }
        }

        .copy-toast {
            position: fixed;
            left: 24px;
            right: 24px;
            bottom: calc(var(--tab-height) + env(safe-area-inset-bottom) + 16px);
            background: var(--text);
            color: #fff;
            padding: 12px 18px;
            border-radius: var(--radius);
            font-size: 14px;
            font-weight: 500;
            text-align: center;
            opacity: 0;
            transform: translateY(8px);
            transition: opacity 0.25s, transform 0.25s;
            z-index: 100;
            pointer-events: none;
            box-shadow: 0 4px 20px rgba(41,27,64,0.2);
        }
        .copy-toast.show {
            opacity: 1;
            transform: translateY(0);
        }

        .tab-item-wrap { position: relative; }
        .cart-tab-badge {
            position: absolute;
            top: -2px;
            right: 50%;
            margin-right: -28px;
            min-width: 18px;
            height: 18px;
            padding: 0 5px;
            font-size: 11px;
            font-weight: 600;
            color: #fff;
            background: var(--tint);
            border-radius: 9px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
        }
        .tab-bar {
            position: fixed;
            bottom: 0;
            left: 0;
            right: 0;
            height: calc(var(--tab-height) + env(safe-area-inset-bottom));
            padding-bottom: env(safe-area-inset-bottom);
            background: rgba(248,246,251,0.94);
            backdrop-filter: saturate(180%) blur(16px);
            -webkit-backdrop-filter: saturate(180%) blur(16px);
            border-top: 1px solid var(--separator);
            display: flex;
            z-index: 50;
        }
        .tab-item {
            flex: 1;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            gap: 4px;
            text-decoration: none;
            color: var(--text-tertiary);
            font-size: 10px;
            font-weight: 500;
            letter-spacing: 0.01em;
            min-height: 48px;
            -webkit-tap-highlight-color: transparent;
        }
        .tab-item.active { color: var(--tint); font-weight: 600; }
        .tab-item svg { width: 22px; height: 22px; }
        .tab-item.active svg { stroke: var(--tint); }
        .guest-cta {
            background: linear-gradient(135deg, rgba(106,89,235,0.18), rgba(106,89,235,0.08));
            border: 1px solid rgba(106,89,235,0.3);
            border-radius: var(--radius);
            padding: 14px 18px;
            margin: 0 16px 16px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-wrap: wrap;
            gap: 12px;
        }
        .guest-cta p { font-size: 14px; font-weight: 500; color: var(--text); margin: 0; }
        .guest-cta a {
            display: inline-flex;
            align-items: center;
            padding: 10px 18px;
            background: var(--tint);
            color: #fff;
            border-radius: 12px;
            font-size: 14px; font-weight: 600;
            text-decoration: none;
        }
        .guest-cta a:hover { opacity: 0.95; }
    </style>
</head>
<body>

<header class="app-header" style="justify-content:space-between;">
    <div style="display:flex;align-items:center;gap:10px;min-width:0;">
        <% if (typeof store !== 'undefined' && store && store.logoUrl) { %>
        <img src="<%= store.logoUrl %>" alt="Store logo" style="width:28px;height:28px;border-radius:8px;border:1px solid rgba(53,38,78,0.12);background:#fff;object-fit:contain;padding:3px;flex-shrink:0;">
        <% } %>
        <h1 class="app-title"><%= (typeof store !== 'undefined' && store && store.name) ? store.name : 'Products' %></h1>
    </div>
    <% if (typeof user !== 'undefined' && user) { %>
    <a href="/logout?next=<%= encodeURIComponent(canonicalLink) %>" style="font-size:13px;font-weight:500;color:var(--text-secondary);text-decoration:none;">Log out</a>
    <% } else { %>
    <a href="/login?next=<%= encodeURIComponent(canonicalLink) %>" style="font-size:13px;font-weight:500;color:var(--tint);text-decoration:none;">Log in</a>
    <% } %>
</header>
<% if (typeof storePath !== 'undefined' && storePath) { %>
<div style="padding:10px 20px;border-bottom:1px solid rgba(53,38,78,0.12);font-size:13px;color:#5c5369;background:rgba(248,246,251,0.7);display:flex;align-items:center;justify-content:space-between;gap:10px;flex-wrap:wrap;">
    <span>Viewing store products</span>
    <a href="<%= storePath %>" style="color:#6a59eb;text-decoration:none;font-weight:600;">Open store</a>
</div>
<% } %>

<% if (typeof user === 'undefined' || !user) { %>
<div class="guest-cta">
    <p>Create an account to add this to cart and checkout.</p>
    <a href="/signup?next=<%= typeof canonicalLink !== 'undefined' ? encodeURIComponent(canonicalLink) : '' %>">Sign up free</a>
</div>
<% } %>

<main class="app-content">
    <div class="immersive-stage">
        <div class="stage-head">
            <p class="result-label">Products</p>
            <p class="result-count">1 item</p>
            <div class="progress-dots"><span class="active"></span></div>
        </div>
        <div class="stack-wrapper">
            <div class="stack-container">
                <div class="ghost-card back-2" aria-hidden="true"></div>
                <div class="ghost-card back-1" aria-hidden="true"></div>
                <div class="product-card">
                    <div class="card-image-fill">
                        <% if (item.type === 'video') { %>
                            <img class="card-image-bg" src="<%= previewImage %>" alt="" aria-hidden="true">
                            <video class="card-video-main" controls playsinline preload="metadata" poster="<%= previewImage %>">
                                <source src="<%= rawMediaUrl %>" type="video/mp4">
                            </video>
                        <% } else { %>
                            <img class="card-image-bg" src="<%= previewImage %>" alt="" aria-hidden="true">
                            <img class="card-image-main" src="<%= previewImage %>" alt="Product">
                        <% } %>
                    </div>
                    <div class="card-top-row">
                        <span class="card-label <%= item.badgeLabel ? '' : 'is-empty' %>"><%= item.badgeLabel || 'LABEL' %></span>
                        <svg class="card-heart" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                            <path d="M20.8 5.6a5 5 0 00-7.1 0L12 7.3l-1.7-1.7a5 5 0 00-7.1 7.1L12 21.5l8.8-8.8a5 5 0 000-7.1z"></path>
                        </svg>
                    </div>
                    <div class="card-bottom-meta">
                        <div class="card-price-inline"><%= item.price %></div>
                        <div class="card-name-inline"><%= (function(){ var s=function(v){ return (v||'').split(',')[0].trim(); }; return [item.categoryName, item.mediaCount > 1 && (item.mediaCount + ' media'), item.size && ('Size: ' + s(item.size)), item.color && ('Color: ' + s(item.color)), item.qty && ('Qty: ' + s(item.qty))].filter(Boolean).join(' · ') || '—'; })() %></div>
                    </div>
                </div>
                <div class="stack-overlay">
                    <div class="stack-spacer" aria-hidden="true"></div>
                    <div class="stack-footer">
                        <div class="card-details">
                            <div class="card-subtitle">Item 1 of 1 — Scroll or swipe to browse. Tap Share to post.</div>
                        </div>
                        <div class="action-row">
                            <button type="button" class="btn-copy" id="copyBtn">
                                <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="9" y="9" width="13" height="13" rx="2"/><path d="M5 15H4a2 2 0 01-2-2V4a2 2 0 012-2h9a2 2 0 012 2v1"/></svg>
                                Copy
                            </button>
                            <button type="button" class="wa-btn" id="shareBtn">
                                <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 12v8a2 2 0 002 2h12a2 2 0 002-2v-8"/><polyline points="16 6 12 2 8 6"/><line x1="12" y1="2" x2="12" y2="15"/></svg>
                                Share
                            </button>
                            <button type="button" class="btn-cart" id="addToCartBtn">
                                <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="9" cy="21" r="1"/><circle cx="20" cy="21" r="1"/><path d="M1 1h4l2.68 13.39a2 2 0 002 1.61h9.72a2 2 0 002-1.61L23 6H6"/></svg>
                                Add to cart
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="copy-toast" id="copyToast">Link copied</div>
</main>

<nav class="tab-bar">
    <% if (typeof role !== 'undefined' && role === 'owner') { %>
    <a href="/" class="tab-item"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="M3 9h18M9 21V9"/></svg><span>Studio</span></a>
    <% } else if (typeof canCreateStore !== 'undefined' && canCreateStore) { %>
    <a href="<%= typeof createStoreHref === 'string' && createStoreHref ? createStoreHref : '/signup?next=%2Fcreate-store' %>" class="tab-item"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="9"/><path d="M12 8v8M8 12h8"/></svg><span>Create</span></a>
    <% } else if (typeof user === 'undefined' || !user) { %>
    <a href="/login" class="tab-item"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 21v-2a4 4 0 00-4-4H8a4 4 0 00-4 4v2"/><circle cx="12" cy="7" r="4"/></svg><span>Log in</span></a>
    <% } %>
    <a href="/products" class="tab-item active"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 2L3 6v14a2 2 0 002 2h14a2 2 0 002-2V6l-3-4z"/><path d="M3 6h18M16 10a4 4 0 01-8 0"/></svg><span>Products</span></a>
    <a href="/cart" class="tab-item tab-item-wrap"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="9" cy="21" r="1"/><circle cx="20" cy="21" r="1"/><path d="M1 1h4l2.68 13.39a2 2 0 002 1.61h9.72a2 2 0 002-1.61L23 6H6"/></svg><span>Cart</span><span class="cart-tab-badge" id="cartTabBadge" style="display:none;">0</span></a>
</nav>

<script>
(function () {
    var payload = <%- JSON.stringify({
        link: canonicalLink,
        price: item.price,
        id: publicId,
        previewUrl: typeof previewImage !== 'undefined' ? previewImage : (typeof rawMediaUrl !== 'undefined' ? rawMediaUrl : '')
    }) %>;
    var shareText = payload.price + '\n' + payload.link;
    var userSignedIn = <%- JSON.stringify(typeof user !== 'undefined' && !!user) %>;

    function showToast(text) {
        var toast = document.getElementById('copyToast');
        toast.textContent = text;
        toast.classList.add('show');
        setTimeout(function () { toast.classList.remove('show'); }, 1800);
    }

    function parsePriceToKobo(priceStr) {
        if (!priceStr || typeof priceStr !== 'string') return 0;
        var num = ('' + priceStr).replace(/[^\d.]/g, '').trim();
        if (!num) return 0;
        var n = parseFloat(num);
        return isNaN(n) ? 0 : Math.round(n * 100);
    }

    function parseMoneyNumber(value) {
        if (value == null) return null;
        var raw = String(value).trim();
        if (!raw) return null;
        var cleaned = raw.replace(/[, ]+/g, '').replace(/[^\d.-]/g, '');
        if (!cleaned || cleaned === '-' || cleaned === '.' || cleaned === '-.') return null;
        var n = Number(cleaned);
        if (!Number.isFinite(n) || n <= 0) return null;
        return n;
    }

    function normalizePriceText(value) {
        var raw = String(value || '').trim();
        if (!raw) return '';
        var n = parseMoneyNumber(raw);
        if (n == null) return raw;
        var hasDecimals = Math.abs(n - Math.round(n)) > 0.0001;
        return '₦' + n.toLocaleString('en-NG', {
            minimumFractionDigits: 0,
            maximumFractionDigits: hasDecimals ? 2 : 0
        });
    }

    payload.price = normalizePriceText(payload.price || '');
    shareText = payload.price + '\n' + payload.link;
    var priceEl = document.querySelector('.card-price-inline');
    if (priceEl) priceEl.textContent = payload.price || '';

    function updateCartBadge() {
        var el = document.getElementById('cartTabBadge');
        if (!el) return;
        var n = (JSON.parse(localStorage.getItem('wa_cart') || '[]')).length;
        el.textContent = n;
        el.style.display = n ? 'inline-flex' : 'none';
    }

    function copyShareText() {
        if (navigator.clipboard && navigator.clipboard.writeText) {
            return navigator.clipboard.writeText(shareText);
        }
        return new Promise(function (resolve, reject) {
            try {
                var textarea = document.createElement('textarea');
                textarea.value = shareText;
                textarea.setAttribute('readonly', '');
                textarea.style.position = 'absolute';
                textarea.style.left = '-9999px';
                document.body.appendChild(textarea);
                textarea.select();
                document.execCommand('copy');
                document.body.removeChild(textarea);
                resolve();
            } catch (err) {
                reject(err);
            }
        });
    }

    document.getElementById('copyBtn').addEventListener('click', function () {
        copyShareText().then(function () {
            showToast('Link copied');
        }).catch(function () {
            showToast('Copy failed');
        });
    });

    document.getElementById('shareBtn').addEventListener('click', async function () {
        try {
            if (navigator.share) {
                await navigator.share({ text: shareText });
            } else {
                window.open('https://wa.me/?text=' + encodeURIComponent(shareText));
            }
        } catch (err) {
            if (err && err.name === 'AbortError') return;
            showToast('Share failed');
            return;
        }
        showToast('Shared');
    });

    document.getElementById('addToCartBtn').addEventListener('click', function () {
        var raw = localStorage.getItem('wa_cart');
        var items = raw ? JSON.parse(raw) : [];
        var amountKobo = parsePriceToKobo(payload.price);
        items.push({
            id: payload.id || payload.link || Math.random().toString(36).slice(2),
            price: normalizePriceText(payload.price || '') || 'Contact for price',
            amountKobo: amountKobo,
            link: payload.link || '',
            previewUrl: payload.previewUrl || ''
        });
        localStorage.setItem('wa_cart', JSON.stringify(items));
        if (userSignedIn) {
            fetch('/api/cart', { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ items: items }), credentials: 'same-origin' }).catch(function () {});
        }
        updateCartBadge();
        showToast('Added to cart');
    });

    updateCartBadge();
})();
</script>
</body>
</html>
