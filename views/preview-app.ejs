<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, viewport-fit=cover, user-scalable=no">
    <meta name="theme-color" content="#0a0a0b">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <title><%= item.price %> · Product</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg: #0a0a0b;
            --bg-elevated: #141416;
            --surface: #1c1c1e;
            --border: rgba(255,255,255,0.08);
            --text: #f5f5f7;
            --text-secondary: #a1a1a6;
            --text-muted: #6e6e73;
            --accent: #0a84ff;
            --accent-hover: #409cff;
            --green: #30d158;
            --radius: 16px;
            --radius-lg: 24px;
            --shadow: 0 4px 24px rgba(0,0,0,0.4);
            --safe-top: env(safe-area-inset-top);
            --safe-bottom: env(safe-area-inset-bottom);
        }
        * { margin: 0; padding: 0; box-sizing: border-box; -webkit-tap-highlight-color: transparent; }
        html { height: 100%; }
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Outfit', sans-serif;
            background: var(--bg);
            color: var(--text);
            min-height: 100%;
            min-height: 100dvh;
            padding: 0 0 calc(32px + var(--safe-bottom)) 0;
        }
        .header {
            position: sticky;
            top: 0;
            z-index: 10;
            padding: calc(12px + var(--safe-top)) 20px 12px;
            background: linear-gradient(180deg, var(--bg) 0%, transparent 100%);
            display: flex;
            align-items: center;
            justify-content: space-between;
        }
        .header-title {
            font-size: 15px;
            font-weight: 600;
            letter-spacing: -0.02em;
            color: var(--text-secondary);
        }
        .header-link {
            font-size: 14px;
            font-weight: 500;
            color: var(--accent);
            text-decoration: none;
        }
        .header-link:hover { color: var(--accent-hover); }
        .hero {
            max-width: 480px;
            margin: 0 auto;
            padding: 8px 20px 24px;
        }
        .card {
            background: var(--surface);
            border: 0.5px solid var(--border);
            border-radius: var(--radius-lg);
            overflow: hidden;
            box-shadow: var(--shadow);
            margin-bottom: 20px;
        }
        .card-media {
            position: relative;
            width: 100%;
            aspect-ratio: 2/3;
            background: var(--bg-elevated);
        }
        .card-media img,
        .card-media video {
            display: block;
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        .card-media video { object-fit: contain; }
        .card-body { padding: 20px; }
        .label { font-size: 11px; font-weight: 600; color: var(--text-muted); letter-spacing: 0.06em; text-transform: uppercase; margin-bottom: 6px; }
        .price { font-size: 28px; font-weight: 700; letter-spacing: -0.03em; color: var(--text); line-height: 1.2; }
        .actions { display: flex; flex-direction: column; gap: 12px; margin-top: 24px; }
        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            width: 100%;
            padding: 16px 24px;
            border-radius: var(--radius);
            border: none;
            font-size: 16px;
            font-weight: 600;
            font-family: inherit;
            cursor: pointer;
            transition: transform 0.15s ease, opacity 0.2s;
        }
        .btn:active { transform: scale(0.98); opacity: 0.9; }
        .btn .icon { width: 20px; height: 20px; flex-shrink: 0; }
        .btn-primary {
            background: var(--accent);
            color: #fff;
        }
        .btn-primary:hover { background: var(--accent-hover); }
        .btn-secondary {
            background: var(--bg-elevated);
            color: var(--text);
            border: 0.5px solid var(--border);
        }
        .btn-secondary:hover { background: rgba(255,255,255,0.06); }
        .btn-wa {
            background: var(--green);
            color: #fff;
        }
        .btn-row {
            display: flex;
            gap: 10px;
        }
        .btn-row .btn { flex: 1; padding: 14px 18px; font-size: 15px; }
        .toast {
            position: fixed;
            left: 20px;
            right: 20px;
            bottom: calc(24px + var(--safe-bottom));
            background: var(--surface);
            color: var(--text);
            padding: 14px 20px;
            border-radius: var(--radius);
            font-size: 15px;
            font-weight: 500;
            text-align: center;
            box-shadow: var(--shadow);
            border: 0.5px solid var(--border);
            opacity: 0;
            transform: translateY(12px);
            transition: opacity 0.25s, transform 0.25s;
            z-index: 100;
            pointer-events: none;
        }
        .toast.show { opacity: 1; transform: translateY(0); }
        .toast a { color: var(--accent); text-decoration: none; font-weight: 600; }
        .footer-links {
            max-width: 480px;
            margin: 0 auto;
            padding: 0 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 24px;
            flex-wrap: wrap;
        }
        .footer-links a {
            font-size: 14px;
            font-weight: 500;
            color: var(--text-secondary);
            text-decoration: none;
        }
        .footer-links a:hover { color: var(--text); }
    </style>
</head>
<body>

<header class="header">
    <span class="header-title">Product</span>
    <a href="/" class="header-link">Studio</a>
</header>

<main class="hero">
    <article class="card">
        <div class="card-media">
            <% if (item.type === 'video') { %>
                <video controls playsinline><source src="<%= rawMediaUrl %>" type="video/mp4"></video>
            <% } else { %>
                <img src="<%= previewImage %>" alt="Product">
            <% } %>
        </div>
        <div class="card-body">
            <div class="label">Price</div>
            <div class="price"><%= item.price %></div>
            <div class="actions">
                <button type="button" class="btn btn-primary" id="addToCartBtn">
                    <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="9" cy="21" r="1"/><circle cx="20" cy="21" r="1"/><path d="M1 1h4l2.68 13.39a2 2 0 002 1.61h9.72a2 2 0 002-1.61L23 6H6"/></svg>
                    Add to cart
                </button>
                <div class="btn-row">
                    <button type="button" class="btn btn-secondary" id="copyBtn">
                        <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="9" y="9" width="13" height="13" rx="2"/><path d="M5 15H4a2 2 0 01-2-2V4a2 2 0 012-2h9a2 2 0 012 2v1"/></svg>
                        Copy link
                    </button>
                    <button type="button" class="btn btn-wa" id="shareBtn">
                        <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 12v8a2 2 0 002 2h12a2 2 0 002-2v-8"/><polyline points="16 6 12 2 8 6"/><line x1="12" y1="2" x2="12" y2="15"/></svg>
                        Share
                    </button>
                </div>
            </div>
        </div>
    </article>
    <div class="footer-links">
        <a href="/">Open Studio</a>
        <a href="/products">Products</a>
        <a href="/cart">Cart</a>
    </div>
</main>

<div class="toast" id="toast"></div>

<script>
(function() {
    var payload = <%- JSON.stringify({
        link: canonicalLink,
        price: item.price,
        id: publicId,
        previewUrl: typeof previewImage !== 'undefined' ? previewImage : (typeof rawMediaUrl !== 'undefined' ? rawMediaUrl : '')
    }) %>;
    var shareText = payload.price + '\n' + payload.link;

    function parsePriceToKobo(priceStr) {
        if (!priceStr || typeof priceStr !== 'string') return 0;
        var num = ('' + priceStr).replace(/[^\d.]/g, '').trim();
        if (!num) return 0;
        var n = parseFloat(num);
        return isNaN(n) ? 0 : Math.round(n * 100);
    }

    function showToast(html) {
        var el = document.getElementById('toast');
        el.innerHTML = html;
        el.classList.add('show');
        setTimeout(function() { el.classList.remove('show'); }, 2200);
    }

    document.getElementById('copyBtn').onclick = function() {
        navigator.clipboard.writeText(shareText).then(function() {
            showToast('Link copied');
        });
    };

    document.getElementById('shareBtn').onclick = function() {
        window.open('https://wa.me/?text=' + encodeURIComponent(shareText));
    };

    document.getElementById('addToCartBtn').onclick = function() {
        var raw = localStorage.getItem('wa_cart');
        var items = raw ? JSON.parse(raw) : [];
        var amountKobo = parsePriceToKobo(payload.price);
        items.push({
            id: payload.id || payload.link,
            price: payload.price || 'Contact for price',
            amountKobo: amountKobo,
            link: payload.link,
            previewUrl: payload.previewUrl || ''
        });
        localStorage.setItem('wa_cart', JSON.stringify(items));
        showToast('Added to cart · <a href="/cart">View cart</a>');
    };
})();
</script>
</body>
</html>
