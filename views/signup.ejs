<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign up</title>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --text: #2d2640;
            --text-secondary: #6b6378;
            --tint: #6a59eb;
            --radius: 20px;
        }
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: 'Outfit', sans-serif;
            min-height: 100dvh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 24px;
            position: relative;
            overflow: hidden;
        }
        .bg {
            position: fixed;
            inset: 0;
            background: linear-gradient(160deg, #b8a9d4 0%, #a8c5e0 45%, #c9b8e0 100%);
            z-index: 0;
        }
        .bg::after {
            content: '';
            position: absolute;
            inset: 0;
            background-image: radial-gradient(circle at 20% 30%, rgba(255,255,255,0.4) 0%, transparent 1.2%),
                radial-gradient(circle at 80% 70%, rgba(255,255,255,0.35) 0%, transparent 1.2%),
                radial-gradient(circle at 40% 80%, rgba(255,255,255,0.3) 0%, transparent 1%);
            background-size: 48px 48px, 56px 56px, 40px 40px;
            background-position: 0 0, 8px 8px, 16px 16px;
            pointer-events: none;
        }
        .card {
            position: relative;
            z-index: 1;
            width: 100%;
            max-width: 360px;
            padding: 36px 28px;
            border-radius: var(--radius);
            background: rgba(255, 255, 255, 0.25);
            backdrop-filter: blur(24px);
            -webkit-backdrop-filter: blur(24px);
            border: 1px solid rgba(255, 255, 255, 0.5);
            box-shadow: 0 16px 48px rgba(45, 38, 64, 0.12), inset 0 1px 0 rgba(255, 255, 255, 0.6);
        }
        .headline { font-size: 26px; font-weight: 700; color: var(--text); margin-bottom: 6px; letter-spacing: -0.03em; }
        .sub { font-size: 15px; color: var(--text-secondary); margin-bottom: 26px; }
        .form-group { margin-bottom: 14px; }
        .form-group input {
            width: 100%;
            padding: 14px 16px;
            font-size: 15px;
            border: 1px solid rgba(255, 255, 255, 0.6);
            border-radius: 14px;
            background: rgba(255, 255, 255, 0.4);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            outline: none;
            color: var(--text);
        }
        .form-group input::placeholder { color: var(--text-secondary); }
        .form-group input:focus {
            border-color: rgba(255, 255, 255, 0.9);
            background: rgba(255, 255, 255, 0.55);
        }
        .btn-row { display: flex; gap: 12px; margin-top: 22px; }
        .btn {
            flex: 1;
            padding: 16px;
            font-size: 16px;
            font-weight: 600;
            color: var(--text);
            background: rgba(255, 255, 255, 0.85);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.7);
            border-radius: 14px;
            cursor: pointer;
            font-family: inherit;
            text-align: center;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 4px 16px rgba(45, 38, 64, 0.08);
        }
        .btn:hover { background: rgba(255, 255, 255, 0.95); }
        .btn-primary {
            background: linear-gradient(135deg, #6a59eb, #8577ee);
            color: #fff;
            border-color: rgba(255, 255, 255, 0.35);
            box-shadow: 0 6px 20px rgba(106, 89, 235, 0.35);
        }
        .btn-primary:hover { opacity: 0.95; }
        .btn-outline { background: transparent; color: var(--text); border: 2px solid rgba(106, 89, 235, 0.6); }
        .btn-outline:hover { background: rgba(106, 89, 235, 0.08); }
        .error { background: rgba(254, 242, 242, 0.9); color: #b91c1c; padding: 12px 14px; border-radius: 12px; font-size: 13px; margin-bottom: 18px; }
    </style>
</head>
<body>
    <div class="bg"></div>
    <div class="card">
        <h1 class="headline">Join us</h1>
        <p class="sub">Create an account to add to cart and checkout.</p>
        <% if (typeof error === 'string' && error) { %><div class="error"><%= error %></div><% } %>
        <form method="post" action="/signup" id="signupForm">
            <div class="form-group">
                <input type="email" id="email" name="email" required placeholder="Enter your email" autocomplete="email">
            </div>
            <div class="form-group">
                <input type="text" id="displayName" name="displayName" placeholder="Your name (optional)" autocomplete="name">
            </div>
            <div class="form-group">
                <input type="password" id="password" name="password" required placeholder="Password (min 6 characters)" autocomplete="new-password" minlength="6">
            </div>
            <div class="btn-row">
                <button type="submit" class="btn btn-primary">Register</button>
                <a href="/login" class="btn btn-outline">Sign In</a>
            </div>
        </form>
    </div>
</body>
</html>
